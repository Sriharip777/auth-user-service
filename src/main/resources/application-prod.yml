server:
  port: ${PORT:8081}

spring:
  application:
    name: auth-user-service

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://educonnect:educonnect123@educonnect.sjmtr4c.mongodb.net/educonnect?appName=educonnect&connectTimeoutMS=20000&socketTimeoutMS=30000&serverSelectionTimeoutMS=30000}
      database: ${MONGODB_DATABASE:educonnect}
      auto-index-creation: true

    redis:
      host: ${REDIS_HOST:redis-15043.c1.us-central1-2.gce.cloud.redislabs.com}
      port: ${REDIS_PORT:15043}
      password: ${REDIS_PASSWORD:Redies@123}
      timeout: ${REDIS_TIMEOUT:6000ms}
      database: 0
      ssl:
        enabled: ${REDIS_SSL:true}
      lettuce:
        pool:
          max-active: ${REDIS_POOL_MAX_ACTIVE:10}
          max-idle: ${REDIS_POOL_MAX_IDLE:5}
          min-idle: 2

  kafka:
    enabled: ${KAFKA_ENABLED:false}

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

eureka:
  client:
    enabled: ${EUREKA_ENABLED:true}
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
    registry-fetch-interval-seconds: 30
    register-with-eureka: true
    fetch-registry: true

  instance:
    # 🔴 KEY FIX: MUST be false on Cloud Run — internal IP is not reachable
    prefer-ip-address: ${EUREKA_PREFER_IP_ADDRESS:false}

    # 🔴 KEY FIX: Register the public Cloud Run hostname, not internal IP
    hostname: ${EUREKA_INSTANCE_HOSTNAME:localhost}
    instance-id: ${spring.application.name}:${random.value}

    # 🔴 KEY FIX: Register as HTTPS (port 443) — Cloud Run only serves HTTPS
    non-secure-port-enabled: false
    secure-port-enabled: true
    secure-port: 443

    # Health check URLs using public Cloud Run hostname
    home-page-url: https://${eureka.instance.hostname}/
    status-page-url: https://${eureka.instance.hostname}/actuator/info
    health-check-url: https://${eureka.instance.hostname}/actuator/health

    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30

jwt:
  secret: ${JWT_SECRET:my-super-secret-jwt-key-for-educonnect-platform-minimum-256-bits-required}
  expiration: ${JWT_EXPIRATION:86400000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}

app:
  two-factor:
    enabled: true
  password-reset:
    token-expiration: 3600000
  frontend:
    url: ${FRONTEND_URL:http://localhost:5173}

services:
  notification:
    url: ${NOTIFICATION_SERVICE_URL:http://localhost:8085}
  learning:
    url: ${LEARNING_SERVICE_URL:http://localhost:8084}

logging:
  level:
    root: INFO
    com.tcon.authuserservice: DEBUG
    org.springframework.cloud.loadbalancer: WARN
    io.github.resilience4j: INFO
    reactor.netty: WARN
    io.netty: ERROR
    org.springframework.data.redis: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
